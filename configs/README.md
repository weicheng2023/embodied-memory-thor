# Configuration

`tasks.yaml` contains the validated Phase 2 task definitions. Each task declares its name, natural-language instruction, required object types, metadata-based goal conditions, and step limit.

Scene and planner configuration files will be added only when later phases require them.
